# Example Streamlit secrets (do not commit real secrets)

# Backend tool_call_handler endpoint (local)
BACKEND_URL = "http://localhost:7071/api/tool_call_handler"

# Optional: set if you route OpenAI calls via proxy (usually "none" locally)
OPENAI_KEY_TRANSPORT = "none"

