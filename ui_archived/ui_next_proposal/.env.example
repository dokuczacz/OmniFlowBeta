# Example env for ui_next/ (do not commit real secrets)

# Base URL of the Azure Functions host (no trailing slash).
# The UI will call `${NEXT_PUBLIC_BACKEND_URL}/api/tool_call_handler`.
NEXT_PUBLIC_BACKEND_URL=http://localhost:7071

